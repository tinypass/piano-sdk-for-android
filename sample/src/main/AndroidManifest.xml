<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.piano.android.sample">

    <application
        android:name=".PianoSampleApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".feature.composer.ComposerActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="Lorem Ipsum"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".feature.composer.ComposerScrollDepthActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="Lorem Ipsum"
            android:theme="@style/AppTheme.NoActionBar" />

        <!--Uncomment this for PianoID social supporting -->
        <!--<activity android:name="io.piano.android.oauth.ui.activity.PianoIdOAuthRedirectActivity">-->
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.VIEW" />-->

                <!--<category android:name="android.intent.category.DEFAULT" />-->
                <!--<category android:name="android.intent.category.BROWSABLE" />-->

                <!--<data-->
                    <!--android:host="success"-->
                    <!--android:scheme="<PUBLISHER_AID_LOWERCASE>.piano.id.oauth" />-->
            <!--</intent-filter>-->
        <!--</activity>-->

    </application>

</manifest>
